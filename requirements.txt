# Core
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9
pydantic>=2.6
pydantic-settings>=2.2
python-dotenv>=1.0
requests>=2.31
numpy>=1.26

# PDF, chunking, retrieval
pymupdf>=1.24
langchain-text-splitters>=0.2
chromadb>=0.5
rank_bm25>=0.2.2

# CPU-only torch (avoids pulling in ~1GB+ of unused NVIDIA CUDA packages,
# which was causing OOM on memory-limited hosts like Render's free/starter tiers)
--extra-index-url https://download.pytorch.org/whl/cpu
torch
sentence-transformers>=3.0   # embeddings + cross-encoder reranker

# UI
streamlit>=1.35

# LLM providers (all optional; pick one in .env)
google-genai>=0.3            # LLM_PROVIDER=gemini (free key from Google AI Studio)
openai>=1.30                 # LLM_PROVIDER=openai

# Dev / test / sample data
pytest>=8.0
httpx>=0.27
reportlab>=4.0
